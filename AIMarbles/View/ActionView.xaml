<UserControl x:Class="AIMarbles.View.ActionView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:local="clr-namespace:AIMarbles"
      xmlns:behavior="clr-namespace:AIMarbles.Core.Behavior"
      xmlns:vm="clr-namespace:AIMarbles.ViewModel"
      xmlns:view="clr-namespace:AIMarbles.View" 
      mc:Ignorable="d">
    <Grid>
            <Border>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Border Grid.Row="0" Background="#555555" CornerRadius="8,8,0,0"
                behavior:DragBehavior.IsDraggable="True">
                                    <TextBlock Text="Action Node" Foreground="White" FontWeight="Bold" Margin="5"/>
                                </Border>


                                <StackPanel Grid.Row="1" VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal" Margin="0,2">
                                        <Ellipse Width="8" Height="8" Fill="#FFC83F" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                        <TextBlock Text="Type" Foreground="#CCCCCC" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                        <ComboBox Width="80" SelectedIndex="0" VerticalAlignment="Center">
                                            <ComboBoxItem Content="Display"/> <!--For now the only option -->
                                        </ComboBox>
                                    </StackPanel>

                                    <Rectangle Height="10" Fill="Transparent"/>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,2">
                                        <TextBlock Text="Link" Foreground="#CCCCCC"/>
                                        <Ellipse Width="8" Height="8" Fill="#FFC83F" VerticalAlignment="Center" Margin="5,0,0,0"/>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>
                                    <Setter Property="Padding" Value="10,5,10,5"/>
                                    <Setter Property="BorderBrush" Value="#333333"/>
                                    <Setter Property="BorderThickness" Value="1" />
                                    <Setter Property="Background" Value="#555555" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                            <Setter Property="BorderBrush" Value="Yellow"  />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Border.InputBindings>
                                <MouseBinding MouseAction="LeftDoubleClick"
                                Command="{Binding DataContext.SelectItemCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                CommandParameter="{Binding}"/>
                            </Border.InputBindings>
                        </Border>
    </Grid>
</UserControl>
