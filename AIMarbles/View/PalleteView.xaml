<UserControl x:Class="AIMarbles.View.PalleteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AIMarbles.View"
             mc:Ignorable="d" 
             d:DesignHeight="75">
    <Grid>
        <StackPanel Orientation="Horizontal">
            <Button Content="Add Metronom" Command="{Binding AddMetronomCommand}"/>
            <Button Content="Add Channel" Command="{Binding AddChannelCommand}"/>
            <Button Content="Add Note" Command="{Binding AddNoteCommand}"/>
            <Button Content="Add Operator" Command="{Binding AddOperatorCommand}"/>
            <Button Content="Remove" Command="{Binding RemoveItemCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
